---
import { highlight } from "../utils/highlight";

interface Props {
  class?: string;
}

const rawCode = await Astro.slots.render("default");
const code = highlight(rawCode);
const className = Astro.props.class;
---

<code class={className} set:html={code} />
