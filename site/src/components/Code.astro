---
import { highlight } from "../utils/highlight";

export interface Props {
  source: string;
  class?: string;
}

const { source, class: className } = Astro.props;

const highlighted = highlight(source);
---

<code class={className} set:html={highlighted} />
