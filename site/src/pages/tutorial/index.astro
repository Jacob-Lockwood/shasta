---
import { getCollection } from "astro:content";
const [{ slug }] = (await getCollection("tutorial")).sort(
  (a, b) => a.data.index - b.data.index
);
---

<head>
  <meta http-equiv="refresh" content={`0; url=/tutorial/${slug}`} />
</head>
